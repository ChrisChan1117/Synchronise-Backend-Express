var Client   = require('../src').Client;
var clearbit = new Client();

clearbit.Discovery.search({query: {and: [{twitter_followers: '10000~'}, {type: 'nonprofit'}]}})
  .then(function (search) {
    console.log('Results:', search.results);
  })
  .catch(function (err) {
    console.error(err);
  });

